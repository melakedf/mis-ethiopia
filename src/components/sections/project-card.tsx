import Link from "next/link";import { ArrowUpRight } from "lucide-react";import { projects } from "@/data/projects";
export function ProjectCard({project:p}:{project:typeof projects[number]}){return <Link href={`/projects/${p.slug}`} className="group flex h-full flex-col rounded-2xl border border-slate-200 bg-white p-7 transition hover:-translate-y-1 hover:border-navy/40 hover:shadow-lg"><div className="flex justify-between text-sm font-semibold text-navy">{p.status}<ArrowUpRight size={20}/></div><p className="mt-7 text-sm text-slate-500">{p.location}</p><h3 className="mt-3 grow text-2xl font-semibold leading-tight text-navy-dark">{p.title}</h3><div className="mt-8 border-t border-slate-200 pt-5"><p className="text-4xl font-semibold text-navy">{p.figure}</p><p className="mt-2 text-slate-600">{p.unit}</p><p className="mt-2 text-sm leading-6 text-slate-500">{p.qualifier}</p></div></Link>}
