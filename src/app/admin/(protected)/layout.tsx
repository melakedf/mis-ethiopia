import Link from "next/link";import { requireAdmin } from "@/lib/auth";import { logoutAction } from "../actions";
export default async function ProtectedAdminLayout({children}:{children:React.ReactNode}){await requireAdmin();return <div className="min-h-screen bg-slate-100"><header className="border-b bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><div><div className="font-bold text-navy-dark">MIS Ethiopia CMS</div><div className="text-xs text-slate-500">Neon-powered website administration</div></div><div className="flex gap-3"><Link href="/" className="rounded-lg border px-4 py-2 text-sm font-semibold text-navy">View website</Link><form action={logoutAction}><button className="rounded-lg bg-navy px-4 py-2 text-sm font-semibold text-white">Sign out</button></form></div></div></header>{children}</div>}